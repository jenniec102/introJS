// compare two strings

// different strings
var string1 = "button";
var string2 = "word";

// same strings
// var string1 = "button";
// var string2 = "button";

if (string1 == string2){
  alert("The two strings are both: " + string1);
} else {
  alert("The strings are not the same! They are: " + string1 + " , " + string2);
}
